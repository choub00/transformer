# AlphaTransformer-Next PRD v2.0
*Phase 2: 2026 全息感交互终端与功能逻辑定义*
*作者: AQM Agent | 版本: 2.0 | 日期: 2026-03-28*

---

## 一、产品定位

### 1.1 核心定位

**AlphaTransformer-Next** 是一款闭环式 AI 驱动时空关联投资看板，定位为 2026 年顶级量化研究终端，融合时序预测（SOTA Transformer 架构）与实时模拟交易能力。

**差异化价值**：
- 学术严谨性：严格 anti-leakage 框架，lag-1 特征 + 信号延迟对齐
- AI 解释性：Cross-Asset Attention 权重 → 中文投资简报（Claude API）
- 毕设适配：Transformer 派系（iTransformer + PatchTST），非 Mamba

### 1.2 目标用户

| 用户 | 场景 | 核心诉求 |
|------|------|---------|
| 毕设评委 | 答辩演示 | 模型可解释、界面专业、数据真实 |
| 个人投资者 | 自研策略验证 | Sharpe、回测可视化、T+1 规则 |
| 量化研究员 | 多资产分析 | Cross-Asset 关系图、因子挖掘 |

---

## 二、视觉语言（Visual Identity）

### 2.1 设计体系

```
主色调
├── 深空暗黑（背景）     #0D1117
├── 毛玻璃卡片（容器）    rgba(22,27,34,0.8) + backdrop-filter: blur(20px)
├── 电光蓝（核心强调）   #00D1FF  (品牌色)
├── 极光绿（盈利）       #00FFBD
└── 霓虹红（亏损）       #FF3B30

辅助色
├── 金色（基准线/强调）  #FFD700
├── 深灰（次要文字）     #8B949E
└── 次深灰（禁用/占位）  #6E7681

字体
├── 主字体: Inter（正文）
├── 数字: JetBrains Mono（等宽，数字跳动）
└── 品牌: AlphaTransformer α

圆角
├── 卡片容器: 16px
├── 按钮: 12px
├── 标签: 6px
└── 输入框: 8px
```

### 2.2 毛玻璃卡片（Glassmorphism）

```css
.glass-card {
  background: rgba(22, 27, 34, 0.8);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 16px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
  transition: border-color 0.2s, box-shadow 0.2s;
}

/* 悬停态：发光边框 */
.glass-card:hover {
  border-color: rgba(0, 209, 255, 0.3);
  box-shadow: 0 8px 32px rgba(0, 209, 255, 0.1);
}
```

### 2.3 动效规范

| 动效类型 | 技术实现 | 参数 |
|---------|---------|------|
| 数值跳动 | GSAP number animation | duration: 0.5s, ease: power2.out |
| PnL 呼吸灯 | CSS keyframes + opacity | 1.5s ease-in-out infinite |
| 卡片入场 | CSS opacity + translateY | 0.3s ease, stagger 50ms |
| K 线绘制 | ECharts animation | duration: 800ms |
| 预测流光线 | ECharts lineStyle dashed | stroke-dashoffset animation |
| 背景微粒 | WebGL noise shader | opacity: 0.03, 60fps |
| 按钮按下 | transform scale | scale(0.97) 100ms |

### 2.4 图表配色

```
K 线颜色
├── 上涨: #00FFBD（极光绿），border: #00FFBD
└── 下跌: #FF3B30（霓虹红），border: #FF3B30

预测曲线
├── 线条: #00D1FF（电光蓝）
├── 样式: dashed, width: 2px
└── 区域: rgba(0, 209, 255, 0.15) 渐变填充

权益曲线
├── 策略: linearGradient(#00D1FF → #00FFBD)
└── 基准: #FFD700（金色虚线）

预测区标注
├── 背景: rgba(0, 209, 255, 0.08)
├── 边框: 左侧 #00D1FF 实线
└── 文字: "AQM 预测区" 左上角标注
```

---

## 三、核心用户场景（User Scenarios）

### 场景 A：行情与预测

**用户路径**：Dashboard → 点击股票代码 → K 线 + AI 预测刷新

**交互细节**：
1. 用户点击左侧股票列表中的任意 ticker
2. AbortController 取消旧请求（防止数据覆盖）
3. 右侧 K 线 500ms 内完成重绘
4. K 线末段背景变为蓝色渐变阴影区（标注 "AQM 预测区"）
5. 预测曲线为电光蓝虚线，从 K 线最后一个节点延伸
6. 鼠标悬停显示 tooltip：

```
┌─────────────────────────────────┐
│ 2026-03-27                      │
│ 开盘: $178.52  收盘: $180.25     │
│ 最高: $181.30  最低: $177.90     │
│ 成交量: 52.3M                    │
│─────────────────────────────────│
│ AI 预测 (2026-03-28)            │
│ 预测价: $182.50  置信度: 78%    │
│ 方向: ↑ 看多                    │
└─────────────────────────────────┘
```

### 场景 B：AI 辅助下单

**用户路径**：选择股票 → 查看 AI 信心评分 → 点击买入

**实时数据联动**：
```
左侧面板（AI 评分卡）
┌──────────────────────────┐
│  信心指数    78%        │  ← 渐变进度条
│  ████████░░░░           │
│  AI 模型: AlphaT-2026   │
└──────────────────────────┘

右侧面板（下单表单）
┌──────────────────────────┐
│  贵州茅台 | 策略买入      │
│  现价: ¥1680.00         │  ← 实时价格（真实股价）
│  数量: [  10  ] 股      │
│  ─────────────────────  │
│  预计成本: ¥16,825.20   │
│  手续费(1.5‰): ¥25.24  │
│  ─────────────────────  │
│  总计:    ¥16,850.44   │  ← 按钮按下时数字跳动
│  ─────────────────────  │
│  [████████ 买入]         │  ← 渐变背景，发光边框
└──────────────────────────┘

下单成功后：
• 资金：¥100,000 → ¥83,149.56（实时联动）
• 持仓：立即出现贵州茅台卡片
• PnL：立即显示 0.00（未实现盈亏）
• 成功提示：绿色 Toast，3s 后消失
```

### 场景 C：实时资产动态

**PnL 呼吸灯**：
```css
/* 盈利态：绿色呼吸 */
@keyframes breathe-green {
  0%, 100% { text-shadow: 0 0 8px rgba(0, 255, 189, 0.3); }
  50% { text-shadow: 0 0 20px rgba(0, 255, 189, 0.8); }
}
.pnl-positive {
  color: #00FFBD;
  animation: breathe-green 1.5s ease-in-out infinite;
}

/* 亏损态：红色呼吸 */
@keyframes breathe-red {
  0%, 100% { text-shadow: 0 0 8px rgba(255, 59, 48, 0.3); }
  50% { text-shadow: 0 0 20px rgba(255, 59, 48, 0.8); }
}
.pnl-negative {
  color: #FF3B30;
  animation: breathe-red 1.5s ease-in-out infinite;
}
```

**总资产数字滚动动画**（GSAP）：
```typescript
// 价格波动时触发
gsap.to(totalAssetsElement, {
  textContent: newValue,      // 目标数值
  duration: 0.5,
  ease: 'power2.out',
  snap: { textContent: 0.01 }, // 平滑插值
  onUpdate: function() {
    // 每帧更新显示
    this.targets()[0].textContent =
      formatCurrency(this.targets()[0].textContent)
  }
})
```

---

## 四、功能架构（Functional Architecture）

```
┌─────────────────────────────────────────────────────────────────────┐
│                        AlphaTransformer-Next                        │
│                      2026 全息感交互终端                            │
├─────────────┬─────────────┬─────────────┬─────────────┬───────────────┤
│  Dashboard  │    Trade    │   Portfolio │   AI解读   │   Settings   │
│   看板主页   │   交易终端  │   持仓分析   │   策略解读   │    设置      │
├─────────────┴─────────────┴─────────────┴─────────────┴───────────────┤
│                         前端状态层 (Pinia)                          │
│  accountStore │ tickerStore │ dashboardStore │ predictionStore       │
├─────────────────────────────────────────────────────────────────────┤
│                         API 服务层 (Axios)                         │
│  apiAccount │ apiMarket │ apiTrade │ apiAuto │ apiDashboard          │
│  AbortController 请求取消 │ 错误拦截 → 中文通知                      │
├─────────────────────────────────────────────────────────────────────┤
│                         后端路由层 (FastAPI)                        │
│  /api/v1/dashboard/* │ /api/v1/trade/* │ /api/v1/account/*        │
│  全局异常拦截：任何错误返回 {error, detail, code}，永不 500           │
├─────────────────────────────────────────────────────────────────────┤
│                         推理引擎层                                   │
│  AlphaTransformer-2026 (iTransformer + PatchTST + SpatioTemporal)  │
│  AlphaTransformer (V1, 随机权重/已训练)                              │
│  ModelRegistry：CPU 推理，特征维度校验，结构化错误                    │
├─────────────────────────────────────────────────────────────────────┤
│                         数据层                                      │
│  AccountManager │ BacktestEngine │ RollingNormalizer                 │
│  东方财富杯规则：净值 < 0.92 淘汰 │ T+1 交易 │ 手续费 1.5‰          │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 五、模块功能定义

### M1：Dashboard（看板主页）

```
┌────────────────────────────────────────────────────────────────────┐
│  顶部指标卡（4 × glass-card，间距 16px）                            │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌────────────┐ │
│  │ Sharpe 1.83 │ │ 年化收益 28.5%│ │ 最大回撤-8.3%│ │  胜率 62.3%│ │
│  │  ↑18.3%    │ │  ↑年化       │ │  ↓回撤       │ │  ↑胜率     │ │
│  │  绿色发光   │ │  绿色发光    │ │  红色发光    │ │  绿色发光 │ │
│  └──────────────┘ └──────────────┘ └──────────────┘ └────────────┘ │
├────────────────────────────────────────────────────────────────────┤
│  权益曲线（ECharts，面积图，600px 高）                             │
│  ├── 策略曲线（蓝→绿渐变填充，面积图）                              │
│  ├── 基准曲线（金色虚线，等权基准）                                  │
│  └── 悬停 tooltip：日期、总资产、基准、超额收益                        │
├────────────────────────────────────────────────────────────────────┤
│  AI 股票推荐表（el-table，10 行）                                   │
│  ├── 排名 Badge（Top3 金色渐变）                                    │
│  ├── Ticker（JetBrains Mono，电光蓝）                               │
│  ├── AI 评分（正绿负红，4 位小数）                                  │
│  ├── 置信度进度条（蓝→绿渐变）                                     │
│  └── 方向标签（bullish 绿/bearish 红/neutral 灰）                   │
├────────────────────────────────────────────────────────────────────┤
│  特征重要性（ECharts 水平条形图，Top 6）                            │
│  └── 渐变条形 + 百分比标签                                          │
└────────────────────────────────────────────────────────────────────┘
```

### M2：Trade（交易终端）

```
┌────────────────────────────────────────────────────────────────────┐
│  股票选择栏（横向滚动 ticker 按钮，5 只默认股）                       │
│  └── 点击切换时：AbortController 取消旧请求，500ms 内完成图表重绘      │
├────────────────────────────────────────────────────────────────────┤
│  K 线 + 预测图（ECharts，双 Y 轴）                                  │
│  ├── K 线（实体色：绿涨红跌）                                      │
│  ├── 成交量副图（底部柱状图）                                       │
│  ├── 预测区背景（蓝色半透明阴影，标注 "AQM 预测区"）                 │
│  ├── 预测曲线（电光蓝虚线，渐变面积填充）                           │
│  ├── 时间范围切换（1月/3月/6月/1年/2年）                           │
│  └── 标注线：当前预测分界线（垂直虚线）                              │
├──────────────────────────┬─────────────────────────────────────────┤
│  账户概览（毛玻璃卡片） │  AI 信心指数卡                             │
│  总资产 / 可用资金     │  ████████░░░ 78%                        │
│  持仓市值 / 累计收益   │  AlphaT-2026 · 置信区间 72%~84%          │
├──────────────────────────┴─────────────────────────────────────────┤
│  下单面板（买入/卖出 Tab 切换）                                      │
│  ├── 方向切换（绿买入 Tab / 红卖出 Tab）                            │
│  ├── 价格输入（默认现价，真实股价如 ¥1680）                         │
│  ├── 数量输入 + 快捷按钮（25%/50%/75%/100%）                       │
│  ├── 手续费预览（1.5‰，实时计算）                                  │
│  └── 提交按钮（渐变背景，disabled 态降低透明度）                     │
├────────────────────────────────────────────────────────────────────┤
│  持仓列表（el-table）                                              │
│  ├── Ticker（可点击 → 切换 K 线）                                  │
│  ├── 成本价 / 现价 / 市值                                          │
│  ├── 浮动盈亏（红绿呼吸灯 + 百分比）                               │
│  ├── AI 建议标签                                                   │
│  └── 操作（详情 → 弹窗 / 平仓 → 确认）                            │
├────────────────────────────────────────────────────────────────────┤
│  自动交易开关（Toggle，AI 标志徽章）                                 │
│  └── 开启后每 5 分钟轮询 /auto/signals，自动执行 Top-K              │
└────────────────────────────────────────────────────────────────────┘
```

### M3：Portfolio（持仓与分析）

```
┌────────────────────────────────────────────────────────────────────┐
│  月度收益热力图（ECharts calendar heatmap）                        │
│  ├── 每天一个格子，颜色深浅 = 收益率正负                             │
│  └── 悬停显示：日期、收益率、基准对比                                │
├────────────────────────────────────────────────────────────────────┤
│  资产配置饼图（ECharts pie）                                        │
│  ├── 每只持仓一只扇区，颜色与持仓颜色一致                           │
│  └── 悬停显示：市值、占比、盈亏                                     │
├────────────────────────────────────────────────────────────────────┤
│  持仓汇总统计                                                      │
│  ├── 总持仓数 / 总市值 / 胜率 / 平均持仓时长                        │
│  └── 持仓分布柱状图（按市值排序）                                   │
└────────────────────────────────────────────────────────────────────┘
```

### M4：AI 策略解读（Claude API）

```
┌────────────────────────────────────────────────────────────────────┐
│  AI 策略解读面板                                                   │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │ 🤖 AI 策略解读                                              │ │
│  │                                                               │ │
│  │ 根据 AlphaTransformer 的 Cross-Asset Attention 权重，       │ │
│  │ AQM 对当前持仓做出以下解读：                                 │ │
│  │                                                               │ │
│  │ 【强势关联】                                                 │ │
│  │ · 贵州茅台与招商银行的 Attention 权重为 0.32，              │ │
│  │   高于均值（0.18），表明资金在消费与金融板块间轮动。          │ │
│  │                                                               │ │
│  │ 【风险提示】                                                 │ │
│  │ · 当前组合波动率为 18.5%，接近策略上限（20%）。              │ │
│  │   建议减少 NVDA 敞口至 15%。                               │ │
│  │                                                               │ │
│  │ 【调仓建议】                                                 │ │
│  │ · 基于时序注意力分析，预测短期内科技板块动量减弱，            │ │
│  │   建议将 TSLA 的 10% 仓位转移至医药（JNJ）。               │ │
│  │                                                               │ │
│  │  [流式输出动画效果...]                                      │ │
│  └──────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────┘

技术实现：
1. 前端调用 /api/v1/dashboard/ai-insights（POST）
2. 后端：
   - 提取 AlphaTransformer 最后一层的 Cross-Asset Attention 权重
   - 格式化 JSON prompt → Claude API（流式）
   - 返回 SSE（Server-Sent Events）流式响应
3. 前端：EventSource 接收，逐字渲染（打字机效果）
```

---

## 六、性能与非功能需求

| 指标 | 目标 | 实现方式 |
|------|------|---------|
| 股票切换响应 | < 500ms | AbortController 取消旧请求 + 乐观渲染 |
| K 线首屏加载 | < 1s | Skeleton 骨架屏 + echarts.setOption 增量更新 |
| 权益曲线渲染 | < 200ms | 数据点限制 252 个（1年），降采样 |
| 持仓更新延迟 | < 3s | 轮询 price-update-interval，requestAnimationFrame |
| 前端崩溃恢复 | < 1s | ErrorBoundary 捕获，Toast 提示，数据从 Pinia 恢复 |
| 移动端适配 | 响应式 | CSS Grid `auto-fit` + `minmax(320px, 1fr)` |

---

## 七、中文化清单

所有 UI 文字严格执行全中文，以下为规范词表：

| 英文 | 中文 |
|------|------|
| Dashboard | 仪表盘 |
| Sharpe Ratio | 夏普比率 |
| Max Drawdown | 最大回撤 |
| Win Rate | 胜率 |
| Annual Return | 年化收益率 |
| Total Assets | 总资产 |
| Available Cash | 可用资金 |
| Position | 持仓 |
| Unrealized PnL | 浮动盈亏 |
| Buy / Sell | 买入 / 卖出 |
| Confidence | 置信度 |
| AI Forecast | AI 预测 |
| Strategy Equity | 策略权益 |
| Benchmark | 基准 |
| Feature Importance | 特征重要性 |
| Model Loading | 模型加载中... |
| Insufficient Funds | 资金不足 |
| Order Submitted | 订单已提交 |
| Connection Lost | 连接中断 |

---

## 八、技术债务与迁移计划

| 优先级 | 任务 | 工作量 |
|--------|------|--------|
| P0 | 统一前端 baseURL 端口（8080） | 5min |
| P0 | 添加 Dashboard Pinia store | 2h |
| P1 | 实现 AbortController 请求取消 | 1h |
| P1 | 实现 GSAP 数值滚动动画 | 2h |
| P1 | 实现 PnL 呼吸灯 CSS | 1h |
| P2 | WebGL 背景噪声纹理 | 4h |
| P2 | 月度热力图组件 | 3h |
| P2 | 资产配置饼图 | 2h |
| P3 | Claude API 流式解读 | 4h |
| P3 | 语音播报盈亏（Web Speech API） | 2h |

---

## 九、Phase 3 任务拆解

```
Phase 3: 前端实现与联调（预计 8h）

[Task 1] 环境对齐 + 基础联调（2h）
  ├─ 统一 api/index.ts baseURL: http://localhost:8080
  ├─ 创建 stores/dashboard.ts
  ├─ 验证 /api/v1/health 返回 {"status":"ok"}
  └─ 验证 /api/v1/dashboard/full 返回结构化数据

[Task 2] Dashboard 重构（2h）
  ├─ 重构 DashboardView.vue 接入真实 API
  ├─ 实现指标卡 GSAP 数字动画
  ├─ 实现 EquityCurve ECharts 组件（策略 vs 基准）
  └─ 实现 FeatureImportance ECharts 组件

[Task 3] TradeView 重构（2.5h）
  ├─ 重构 K 线 + 预测图（ECharts）
  ├─ 实现 AbortController 取消逻辑
  ├─ 实现下单表单 + 手续费实时计算
  ├─ 实现账户/持仓实时联动
  └─ 实现 PnL 呼吸灯效果

[Task 4] Portfolio + AI 解读（1.5h）
  ├─ 实现月度热力图（calendar heatmap）
  ├─ 实现资产饼图（pie chart）
  └─ 实现 Claude API 流式解读面板（Stream → SSE）

[Task 5] 验收测试（1h）
  ├─ 股票切换 < 500ms（Chrome DevTools Network）
  ├─ 所有按钮点击有反馈（Toast/Loading）
  └─ 移动端（F12 Mobile 模拟）无溢出
```

---

*本 PRD 为 Phase 2 交付物，涵盖视觉规范、交互细节与 Phase 3 任务拆解。*
*下一步：执行 Phase 3 任务拆解。*
